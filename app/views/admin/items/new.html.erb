<%= form_with model: @item do |f| %>
  <h4>商品名</h4>
    <%= f.text_field :title %>
  <h4>商品説明</h4>
    <%= f.text_area :body %>
  <h4>価格（税抜）</h4>
    <%= f.text_field :price %>
  <%= f.submit 'Create Item', class:"btn btn-success" %>
<% end %>
