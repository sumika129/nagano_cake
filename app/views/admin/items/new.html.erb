<%= form_with model: [:admin,@item] do |f| %>
  <h4>商品名</h4>
    <%= f.text_field :name %>
  <h4>商品説明</h4>
    <%= f.text_area :introduction %>
  <h4>価格（税抜）</h4>
    <%= f.text_field :price %>
  <%= f.submit 'Create Item', class:"btn btn-success" %>
<% end %>
