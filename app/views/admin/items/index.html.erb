<h2>商品一覧</h2>
  <table>
    <thead>
      <tr>
        <th></th>
        <th>商品名</th>
      </tr>
    </thead>
    <% @item.each do |item| %>
    <tbody>
      <tr>
        <td><%= link_to item.name, item_path(item) %></td>
      </tr>
    </tbody>
    <% end %>
  </table>
